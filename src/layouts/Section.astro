---

---

<section>
  <slot />
</section>

<style>
  @layer components {
    section {
      display: grid;
      grid-template-columns: subgrid;
      padding-block: 5rem;
      /* background-color: var(--tertiary-color); */

      > * {
        grid-column: content;
      }
    }
  }
</style>
